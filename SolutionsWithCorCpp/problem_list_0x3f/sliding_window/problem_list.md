一、定长滑动窗口
§1.1 基础

```cc
// 看作是可变窗口的特例
int n = s.size(), ret = 0, cur = 处理第一个元素, r = 0;
for (int i = 0; i < n; i++) {
    while (r + 1 < n && r - i + 1 < k) {
        r++;
        处理s[r]
    }
    if(r - i + 1 == k){
        更新ret
    }
    解除cur[i]的影响;
}
```

1456. 定长子串中元音的最大数目 1263
643. 子数组最大平均数 I
1343. 大小为 K 且平均值大于等于阈值的子数组数目 1317
2090. 半径为 k 的子数组平均值 1358

```cc
// 真正按照定长窗口来处理
// 先处理出一整个k区间[0, k], 然后后面的就可以每次O(1)维护

int cur = 处理当前[0, k]区间, ret = cur, r = k;
for(int i = 1;r < n;i++){
    处理nums[r], 计算对应的cur
    更新ret
    取消nums[i]的影响
    r++;
}
```
    
2379. 得到 K 个黑块的最少涂色次数 1360
2841. 几乎唯一子数组的最大和 1546
2461. 长度为 K 子数组中的最大和 1553    (2461和2841完全一样)
1423. 可获得的最大点数 1574


§1.2 进阶（选做）

3679. 使库存平衡的最少丢弃次数 1639


TODO: 
    1052. 爱生气的书店老板
    3439. 重新安排会议得到最多空余时间 I 1729
    3694. 删除子字符串后不同的终点 1739
    2134. 最少交换次数来组合所有的 1 II 1748
    1652. 拆炸弹 做到 O(n)
    1297. 子串的最大出现次数 1748
    3652. 按策略买卖股票的最佳时机
    1888. 使二进制字符串字符交替的最少反转次数 2006
    567. 字符串的排列
    438. 找到字符串中所有字母异位词
    30. 串联所有单词的子串
    2156. 查找给定哈希值的子串 2063
    2953. 统计完全子字符串 2449
    1016. 子串能表示从 1 到 N 数字的二进制串 做到 O(∣s∣)


二、不定长滑动窗口
不定长滑动窗口主要分为三类：求最长子数组，求最短子数组，求子数组个数。

注：滑动窗口相当于在维护一个队列。右指针的移动可以视作入队，左指针的移动可以视作出队。

§2.1 越短越合法/求最长/最大

```cc
int n = s.size(), r = 0, res = 0;
int hash[256] = {0};
for(int i = 0;r < n;r++){   // 每次r右移, 判断r有效
    hash[s[r]]++;           // s[r]移入窗口
    while(hash[s[r]] > 1 && i < n){ // 只要窗口不符合要求, i就右移
        hash[s[i]]--;
        i++;
    }
    if(i <= r) res = max(res, r - i + 1);    // 需要额外判断当前窗口有效
}
return res;
```

§2.1.1 基础

3. 无重复字符的最长子串
3090. 每个字符最多出现两次的最长子字符串 1329   (和上一题LC3完全一样)
1493. 删掉一个元素以后全为 1 的最长子数组 1423
3634. 使数组平衡的最少移除数目 1453            (需要用到一点预处理技巧)
1208. 尽可能使字符串相等 1497
904. 水果成篮 1516
1695. 删除子数组的最大得分 1529
2958. 最多 K 个重复元素的最长子数组 1535
2024. 考试的最大困扰度 1643
1004. 最大连续 1 的个数 III 1656


§2.1.2 进阶（选做）

TODO: 
    2730. 找到最长的半重复子字符串 非暴力做法
    2779. 数组的最大美丽值 1638
    1658. 将 x 减到 0 的最小操作数 1817
    1838. 最高频元素的频数 1876
    2516. 每种字符至少取 K 个 1948
    2831. 找出最长等值子数组 1976
    2271. 毯子覆盖的最多白色砖块数 2022
    2106. 摘水果 2062
    2555. 两个线段获得的最多奖品 2081
    2009. 使数组连续的最少操作数 2084
    1610. 可见点的最大数目 2147
    2781. 最长合法子字符串的长度 2204
    3411. 最长乘积等价子数组 非暴力做法约 2300
    3413. 收集连续 K 个袋子可以获得的最多硬币数量 2374
    395. 至少有 K 个重复字符的最长子串
    1763. 最长的美好子字符串 非暴力做法
    2968. 执行操作使频率分数最大 2444
    1040. 移动石子直到连续 II 2456


§2.2 越长越合法/求最短/最小

```cc
// 改变一下更新ret的位置
int n = nums.size(), r = 0, ret = INT_MAX;
long long curSum = 0;
for (int i = 0; r < n; r++) {
    curSum += nums[r];
    while (curSum >= target && i < n) {
        if (i <= r) ret = min(ret, r - i + 1);  // 在这里更新ret
        curSum -= nums[i];
        i++;
    }
}
```

209. 长度最小的子数组
3795. 不同元素和至少为 K 的最短子数组长度 1505
2904. 最短且字典序最小的美丽子字符串 做到 O(n^2)
--没做出来    1234. 替换子串得到平衡字符串 1878
2875. 无限数组的最短子数组 1914
76. 最小覆盖子串
--没做出来    632. 最小区间 做法不止一种




§2.3 求子数组个数
§2.3.1 越短越合法
一般要写 ans += right - left + 1。

内层循环结束后，[left,right] 这个子数组是满足题目要求的。由于子数组越短，越能满足题目要求，所以除了 [left,right]，还有 [left+1,right],[left+2,right],…,[right,right] 都是满足要求的。也就是说，当右端点固定在 right 时，左端点在 left,left+1,left+2,…,right 的所有子数组都是满足要求的，这一共有 right−left+1 个。



TODO: 
    713. 乘积小于 K 的子数组
    3258. 统计满足 K 约束的子字符串数量 I 做到 O(n)
    2302. 统计得分小于 K 的子数组数目 1808
    2762. 不间断子数组 1940
    LCP 68. 美观的花束
