## 矩阵快速幂

### 前置知识

#### 1. 同余原理

1. 加法同余: `(a + b) % mod = a % mod + b % mod`
2. 乘法同余: `(a * b) % mod = (a % mod) * (b % mod)`

    需要注意的是, 乘法同余中, 我们需要保证`(a % mod) * (b % mod)`这一步不会发生溢出, 即如果`a`, `b`都是`int`类型, 那么在进行`(a % mod) * (b % mod)`这一步时, 我们转成`long`类型进行相乘, 以此来避免溢出, 因为两个`int`类型相乘的结果显然不会超过`long`类型的范围
3. 减法同余: `(a - b) % mod = (a % mod - b % mod + mod) % mod`

#### 2. 乘法快速幂

考虑这样一个情景: 求`(13 ^ 75) % mod = ?`

如果直接用`int`进行运算, 显然是不行的, 而如果用`Java`中自带的`BitInteger`或者`Python`中的`BitInt`, 确实可以运算, 但是由于大整数的运算时间复杂度较高, 因此会超时

有没有更好的方法? -- 乘法的快速幂

首先将指数`75`拆成二进制形式`75 = 64 + 8 + 2 + 1`, 因此`75 = 0b01001011`, 即`13 ^ 75 = 13 ^ (0b01001011)`, 根据乘法的性质, 我们可以把指数中的每一个二进制位拆出来, 即`13 ^ (0b01001011) = (13 ^ (0b01000000)) * (13 ^ (0b00001000)) * (13 ^ (0b00000010)) * (13 ^ (0b00000001))`

因此我们可以从指数的二进制位的低位开始算, 维护`x`初始值为`13`, `ans`初始为`1`, 遍历指数二进制位的每一位, 如果当前这一位是`1`, 那么`ans *= x`, 否则`ans`保持不变, 并且每遍历一个二进制位时, 都让`x *= x`

为什么是`x *= x`?
    
比如某时刻`x = 13 ^ (0b0010)`, 那么`x *= x`之后, `x = 13 ^ (0b0100)`, 即下一个二进制位对应指数的幂

> 需要注意的是, 根据乘法的同余原理, 在上面的每一步乘法中, 都需要取模

这样我们只需要花费`O(指数n的二进制长度)`即`O(logn)`的时间, 即可计算出原式的答案

coding: 

```java
public static int pow(int base, int exp, int mod){
        long ans = 1, x = base;
        while(exp != 0){
            if((exp & 1) == 1){
                ans = (ans * x) % mod;
            }
            exp >>= 1;
            x = (x * x) % mod;
        }
        return (int)ans;
    }
```

#### 3. 矩阵乘法

